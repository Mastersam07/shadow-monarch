name: shadow_monarch
description: Solo Leveling dungeon crawler — ARISE
publish_to: 'none'
version: 0.1.0

environment:
  sdk: '>=3.0.0 <4.0.0'
  flutter: '>=3.10.0'

dependencies:
  flutter:
    sdk: flutter
  flame: ^1.22.0
  flame_audio: ^2.10.0
  gamepads: ^0.1.5

flutter:
  assets:
    - assets/audio/

dev_dependencies:
  flutter_lints: ^5.0.0
